{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "mern-app.fullname" . }}-frontend-egress-backend
  labels:
    {{- include "mern-app.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "mern-app.frontend.fullname" . }}
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: {{ include "mern-app.backend.fullname" . }}
      ports:
        - protocol: TCP
          port: {{ .Values.backend.service.port }}
{{- end }}
